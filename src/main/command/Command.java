package main.command;

/**
 * This interface represents a command in the TextEditor application, following the Command design pattern.
 *
 * @author Tharith Yeak
 */
public interface Command {
    /**
     * Executes the command.
     */
    void execute();
}
